# Python Runtime Installer 0.1.0

首个预发布版本，提供 Windows 10/11 x64 的离线 CPython 3.13.14 受管环境、14 个锁定直接依赖、事务式验证、双语/静默安装、开始菜单入口和本地日志。

文件名含 `-unsigned` 时表示没有 Authenticode 签名，可能触发 SmartScreen；请在运行前核对 `SHA256SUMS.txt`。首版不包含浏览器或 WebDriver。
